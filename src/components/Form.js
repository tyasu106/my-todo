export const Form = (props) => {
  const { inputTodo, onClickAdd, onChangeInputTodo } = props;
  return (
    <div>
      {/* <form onClick={onClickAdd}>
        <input
          placeHolder="TODOを入力"
          value={inputTodo}
          onChange={onChangeInputTodo}
        />
        <button>追加</button>
      </form> */}
    </div>
  );
};
